# 2026-07-13 — Work log

Scope this session: the **document-preprocessing pipeline** under `tools/`
(Stage 1 dispatcher + Stage 2 normalization). None of this touches core
Kitbash query/sleep planes, so no SOCKET_MAP cells move — the tools live in
the isolation-first `tools/` sandbox and feed the future sleep-input layer
(post-1.0), not the live orchestrator.

## 1. Spec reconciliation — document dispatcher (commit `62d9a39`)
- Found the on-disk `tools/SPEC-document_dispatcher.md` had been updated to
  wire **PDF** into the dispatcher (the prior paste was identical to the old
  file; user re-sent the real update).
- Reconciled three internal contradictions the update introduced:
  1. Extractor-name note said `pdf_extractor`; table said `pdf_to_markdown` →
     unified on `pdf_to_markdown`.
  2. Routing pseudocode generated `tools.pdf_extractor`/`convert_pdf_to_markdown`,
     which doesn't exist (real package is `tools.pdf_to_markdown`). Promoted
     `format_registry.py` from optional to **REQUIRED** with an explicit
     `pdf → ("tools.pdf_to_markdown", "convert_pdf_to_markdown")` override.
  3. Logging example did `len(text)` but `pdf_to_markdown` returns `None` →
     changed logging to read the written output file for `char_count`.
- `.gitignore`: added `*.log` so `ops.log`/`ops.log.1` can't be swept in by
  `git add -A`. (Caught that `*.log` alone missed the rotated `ops.log.1` —
  fixed in commit `3d9eb06` by adding `*.log.*`.)

## 2. Prior-session webui changes reviewed + committed (commits `48edd44`, `9b389d9`)
- `kitbash_web.py`: buffered `Content-Length` response replacing the broken
  manual chunked framing (fixes 0-byte body on client disconnect). Verified
  `py_compile` + no dangling `_send_chunk` refs.
- `scripts/run_webui.sh`: replaced self-restarting watchdog with a disabled
  no-op (user request 2026-07-13); revert via `git checkout`.
- `9b389d9` corrected a stale docstring in `kitbash_web.py` that still claimed
  chunked/progressive streaming (it now buffers). No logic change.
- All four commits (`62d9a39`, `3d9eb06`, `48edd44`, `9b389d9`) pushed to
  `origin/main` (`c70a745..9b389d9`).

## 3. Stage 2 normalization tool built (commit `eef6d59`)
- `tools/stage2_normalization/`: `normalize_text(text) -> str` — line-ending
  normalization (CRLF/LF → LF), blank-line collapse (3+ → 2), trim, and
  exact-match duplicate-line removal (first kept; **blank lines excluded from
  dedup** so paragraph spacing survives). CLI (`-o`, stdin), README, SPEC.
- **Two resolved spec contradictions:** (a) Error Handling said `ValueError` on
  empty string, but test case 5 requires empty → `""`. Followed the test case
  (empty → `""`, exit 0). (b) dedup pseudocode would have deduped blanks too,
  destroying paragraph spacing — excluded blanks from dedup.
- **Verification (ad-hoc, executed, not suite-green):** all 10 SPEC manual
  test cases PASS (happy path, blank collapse, dup keep-first, clean, empty,
  single line, CRLF→LF, missing-file exit 1, None→ValueError, unwritable→exit 1).
  Temp verifier under `%TEMP%`, deleted after.
- **Real bug caught during verify:** `cli.py` referenced `_logger` without
  defining it → `NameError` on every CLI run. Fixed by adding the
  `structured_logger` setup (try/except guarded) matching `core.py`.

## 4. document_dispatcher implemented + Stage 2 wired (commit `f459cfe`)
- Built the long-specced `tools/document_dispatcher/` (was spec-only):
  `format_registry.py` (`resolve_extractor()`, pdf override + default
  `tools.<fmt>_extractor` convention), `core.py` (`extract_document`), `cli.py`
  (`-o/--output`, `--normalize`), `__init__.py`/`__main__.py`, README.
- **Stage 2 wired behind opt-in `--normalize` (default OFF, per user choice):**
  when set, dispatcher runs `tools.stage2_normalization.normalize_text` on the
  extracted file and rewrites it. Raw extractor output preserved otherwise.
- SPEC updated: In-Scope + API (CLI + signature) + Routing Logic document the
  `normalize` flag; test cases 9–10 cover on/off. `tools/README.md` lists the
  dispatcher (also fixed a pre-existing `pdf_to_mark_down`→`pdf_to_markdown`
  usage typo in the pdf_to_markdown line).
- **Verification (ad-hoc, executed):** format detection, routing to
  txt_extractor, `--normalize` output == `normalize_text(raw)`, Stage 2 dedup
  (raw 2 → norm 1), default-OFF preserves raw, missing→FileNotFoundError,
  unknown-ext→ValueError, CLI exit 0 + emits `Detected <fmt>. Converted…`.
  All PASS. Temp verifier deleted.
- **Honest caveats:** (1) `FileNotFoundError` takes precedence over format
  detection (existence checked first) — matches SPEC error table. (2) The text
  extractor prints its own `Converted…` line to stdout before the dispatcher's
  summary; both appear, dispatcher summary is present. (3) 8 format extractors
  follow the `<format>_extractor` convention; only `pdf_to_markdown` is
  grandfathered and handled by the registry override. Not yet tested end-to-end
  against PDF/DOCX/etc. via the dispatcher (only `txt` was exercised live).

## 5. Pushed to origin/main
- All six commits this session: `62d9a39`, `3d9eb06`, `48edd44`, `9b389d9`,
  `eef6d59`, `f459cfe`. `HEAD == origin/main` after push. Working tree clean
  (ops logs remain untracked + now fully gitignored).

## Deliberately NOT done
- Stage 2 default-ON (left opt-in per user).
- Content-sniffing v2 (extension-only detection, per SPEC).
- Fuzzy dedup / boilerplate detection (v2+ non-goals).
- Connecting dispatcher output into Dream Bucket / sleep-input layer (post-1.0).
- Live dispatcher runs against PDF/DOCX/HTML/JSON/etc. (only txt verified).
- No SOCKET_MAP.md / STATE_OF_THE_PROJECT updates: this work is in the isolated
  `tools/` pipeline, postdates the 2026-07-13 freeze doc, and does not alter
  any core socket boundary.
