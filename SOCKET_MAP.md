# KITBASH SOCKET MAP

**Date:** 2026-07-07 (initial pass, from Fable audit of read-only copies)
**Updated:** 2026-07-11 — BitNet routing gap CLOSED (BITNET → GREEN; verified live, see cell). 2026-07-10 reconciliation: SPEC_ORCHESTRATOR_RECONCILIATION Steps 0–6 executed (tasks T1–T8). Cells flipped GREEN where a contract suite passed on real hardware (Query entry, TriageAgent, InferenceEngine:CARTRIDGE, DiagnosticFeed, LearningObserver, Phantom tracker, Resonance weights, MTR state manager, Dream Bucket write, Orchestrator contract suite). MTR engine + MTR contract suite are GREEN (TEST-MTR_v6_1_contract.py executed 10/10 on real hardware 2026-07-10 — see cells). GRAIN/LLM/Epistemic layer names/HatKappaMapper/MTR↔Grain bridge/L2 service: GrainRouter + L2 flipped GREEN later 07-11 (see cells); GRAIN cascade slot, LLM/specialists, Epistemic names, HatKappaMapper, MTR↔Grain bridge unchanged from this summary. **Map-maintenance pass 2026-07-12:** corrected this summary line (MTR was GREEN, not YELLOW) + Critical Path line (stream format GREEN/RATIFIED, RedisBlackboard YELLOW not blank) + Dream Bucket write cell (added 5092d86 trace-persistence root-cause fix, post-dating the prior 21:31 map edit) to stop the map contradicting its own per-cell states.
**Purpose:** The project-level answer to "is it done?" A socket is a module boundary another component plugs into. Each socket has a contract status. **"Done for now" = every socket on the active phase's path is GREEN.** Cells off the path may stay red indefinitely without meaning anything — they are unvisited territory, not failure.

**How this document is used:**
1. Every task's DONE WHEN block names the socket cell(s) it turns green.
2. Discoveries during work file new rows or flip cells red — they never extend the current task (scope-lock rule).
3. A socket may not change interface without its contract suite changing in the same session.
4. Status changes require *executed* test output, pasted. Reading is not verification (see POSTMORTEM_MTR_v6.md).

**Legend:**
- **GREEN** — contract suite exists AND has been executed passing on real hardware
- **YELLOW** — interface defined and in use; no contract suite (works by luck and habit)
- **RED** — measured failing, known broken, or fails soft on contract-bearing keys
- **PLANNED** — socket reserved by design; nothing plugged in yet

---

## 1. Query Plane (the answering path)

| Socket | Interface | Status | Evidence / Notes |
|---|---|---|---|
| Query entry (factory → orchestrator) | `create_query_orchestrator()` → `process_query(str, ctx) -> QueryResult` | **GREEN** | Steps 0–6 complete (T1–T8). Evidence: TEST-orchestrator_e2e.py 10/10 PASS (T8, real torch 2.13.0) — NOTE: e2e harness currently fails to import (pre-existing missing `from pathlib import Path`, reproduced on clean main); the 23/23 contract gate (TEST-orchestrator_contract.py, T7) is the executed boundary proof and is GREEN as of 2026-07-12 (SPEC_CONTRACT_SUITE_MAMBA_REPAIR.md). The earlier "23/23" count was STALE after Pattern A (2026-07-11) until that repair. Query-entry behavior unchanged. F1/F2 resolved: factory builds shared MTR/state/pipeline and passes them; single engine registry. Committed 82a4f31. **Re-validated 2026-07-11 (End-to-End Chat Milestone Phase A):** real `process_query("What is ATP...")` through canonical path returned `QueryResult` (answer via CARTRIDGE, confidence 0.75, no crash); observer fired with live `L2WorkingTheoryService` (Mutation 2 now wired). FINDING: `learning_report.trace_logged=False` on this run — observer's trace logging did not fire for this query; and `mtr_confidence=-7.19` (negative, same unvalidated signal flagged in DIAGNOSTIC-trace_signal_check.py). Both are follow-up findings, not Phase A blockers. |
| TriageAgent | `interfaces/triage_agent.py`: `route(TriageRequest) -> TriageDecision` | **GREEN** | Interface is `route` (ABC declares `route`, not `decide` — orchestrator corrected in T8). Exercised on real hardware: T8 e2e calls `RuleBasedTriageAgent.route()`, cascade honors its layer_sequence (CARTRIDGE answered). Triage sequence+thresholds+ESCALATE asserted in T7 #1. |
| InferenceEngine: GRAIN | `interfaces/inference_engine.py`: `query(InferenceRequest) -> InferenceResponse` | YELLOW | Cascade slot. Interface is `query` (not `infer` — orchestrator corrected in T8). Registry shared via factory (F2, T2). Real `query()` path exercised through cascade in T7/T8 but GRAIN not observed *answering* in e2e (CARTRIDGE selected); stays YELLOW until a GRAIN-answer e2e is captured. |
| InferenceEngine: CARTRIDGE | same | **GREEN** | `query()` path fixed in T8; answered a real query in TEST-orchestrator_e2e.py (CARTRIDGE, conf 0.75, real torch). T7 #1 asserts first-passing-engine-wins through this slot. |
| InferenceEngine: BITNET | same (HTTP to local server) | **GREEN (2026-07-11)** | Server DEPLOYED + reachable: `llama-server.exe` (llama.cpp build) runs `Little-Bitch-3B.i1-Q6_K.gguf` on 127.0.0.1:8080, answers raw `/completion` (verified independently, pre-orchestrator). `enable_bitnet=True` wires `BitNetEngine` into the registry (confirmed `['GRAIN','CARTRIDGE','BITNET']`). ROUTING CLOSED: `rule_based_triage.py` now inserts BITNET into every `layer_sequence` (before ESCALATE) when `enable_bitnet=True`, with a 0.75 confidence threshold (verified end-to-end — live BitNet server answered through the cascade when CARTRIDGE was forced to miss: `BITNET conf=0.75, passed`). CAVEAT: cascade is winner-take-all (first-passing engine wins); in normal runs CARTRIDGE (0.75 ≥ 0.70) passes first, so BITNET fires as fallback only when CARTRIDGE misses. BITNET-conf 0.75 vs CARTRIDGE 0.70 is the lever to make BitNet contribute more often. **COMBINED e2e 2026-07-11:** one orchestrator with `enable_bitnet=True`+`enable_mamba=True` → normal query answers via CARTRIDGE (conf 0.75), forced CARTRIDGE miss answers via BITNET (conf 0.75, BITNET layer present) — BitNet confirmed participating as fallback alongside live Mamba context. **TUNED 2026-07-11 (later turn):** BITNET threshold lowered `0.75 → 0.65` (strictly below CARTRIDGE `0.70`) AND `_insert_bitnet` now places BITNET **before CARTRIDGE** in the cascade (`GRAIN → BITNET → CARTRIDGE → ESCALATE`) so BitNet is evaluated in normal traffic and wins when its confidence ≥ 0.65, instead of only firing on a CARTRIDGE miss. Verified live through the web POC: 7/7 normal queries answered by BITNET (BitNet returns ~0.75 ≥ 0.65). Net effect: both models genuinely engage in normal traffic (BITNET-primary learned net, CARTRIDGE crystallized fallback); CARTRIDGE-dominance eliminated. Not further tuned (stop-at-checkpoint). **RUNTIME CONFIG 2026-07-11 (later turn):** server relaunched with `-c 4096` (was `-c 512`, which capped prompt+generation and caused mid-sentence truncation); `/props` confirms `n_ctx=4096`. **BitNet runs CPU-ONLY** — this is a CPU-only llama.cpp build (no `cuda`/`cublas` DLLs; only `ggml.dll`/`llama.dll`/`llava_shared.dll`), so `-ngl 20` is a **no-op** — verified live: GPU used ~788 MiB with server up vs ~792 MiB killed (≈0 VRAM). This is the intended mode (BitNet = efficient low-bit CPU inference). Memory budget = SYSTEM RAM (weights ~2.2GB + KV-cache ~1280 MiB @ `-c 4096`), NOT VRAM; GPU fully reserved for the eventual quantized synthesis LLM (no contention). Client defaults now `bitnet_max_tokens=256` + engine `timeout_seconds=60` (256 tok ≈ 32s; both configurable via factory `bitnet_max_tokens` / engine ctor). Verified e2e: long query answered by BITNET, 208 words, 32.6s, no timeout fallback. Commits `9f34e10`/`5d359fa`. Server relaunch is a manual terminal action (not persisted) — needs re-run after reboot until a launch script lands. |
| InferenceEngine: LLM / specialists | same | PLANNED | Phase 4+ cascade tail. The "LLM as generation peripheral" socket. |
| MambaContextService | `get_context(MambaContextRequest)` | **GREEN (2026-07-11)** | Real `RealMambaService` now wired (Option B2). `query_orchestrator_factory.py` swaps `MockMambaService()`→`RealMambaService(...)` gated on `enable_mamba` (default False; mock fallback if disabled/construct fails). `real_mamba_service.py` is a TCP client to `bitmamba_server.exe` (persistent C++ shim, model loaded once, built clang-cl). Orchestrator forwards `user_query` into the request (`_get_mamba_context`). **Mapping (Option 1):** `context_1hour` = generated text, `context_1day/72hours/1week` LEFT EMPTY by design (BitMamba stateless), `active_topics` extracted (crude heuristic). Graceful: never None, never raises; disabled/unreachable → empty-but-valid. **VERIFIED:** ad-hoc test against live `bitmamba_server` (255M) → 1hour populated + topics extracted; disabled + unreachable → empty (ALL PASS). Engine lives at `B:\ai\llm\kitbash\bitmamba.cpp` (outside repo). CAVEAT: 3 longer windows empty until stateful memory lands (see SPEC §2b D2) — do not fabricate. **CONSUMED (later 2026-07-11 turns):** Pattern A is LIVE — `context_1hour['generated']` is prepended to the engine prompt as a `[Recent context]` block in `query_orchestrator_posix.py`; `QueryResult.mamba_injected` flag surfaces it via `kitbash_cli.py` (`answer_done`) + web POC badge. Verified live: engine receives the prepended block; web `answer_done.mamba_injected=true`. Pattern B (routing-aware) + C (L2 enrichment) documented deferred in SPEC §11 — NOT implemented. |
| Chat I/O bridge: `kitbash_cli.py` + `kitbash_web.py` POC | CLI stdio JSON (chat `answer_chunk`/`answer_done`/`error` on stdout, ops on stderr) + web `POST /query` streams CLI stdout, `GET /ops` exposes ops | **GREEN (2026-07-11)** | MVP chat surface (not the final product). `kitbash_cli.py` verified: stdout 100% chat JSON, stderr ops, RC=0 (`TEST-kitbash_cli.py` 3/3 PASS). `kitbash_web.py` stdlib POC verified: live `POST /query` streamed ndjson + `GET /ops` returned ops; `answer_done` carries `engine` + `mamba_injected` so both models are visibly engaged. Wire contract: `docs/CLI_PROTOCOL.md`. |
| DiagnosticFeed | `log_query_*`, `log_layer_*`, `log_error`, `log_metric` | **GREEN** | No-op stand-in when Redis absent (acceptable soft-fail: telemetry). Loud failure path verified: observer exception routes to `feed.log_error("LEARNING_OBSERVER", ...)` and does NOT change the answer (T7 #5, executed) + real run in T8. |

## 2. Learning Plane (the observing path)

| Socket | Interface | Status | Evidence / Notes |
|---|---|---|---|
| LearningObserver | `observe(query_id, query, ctx, result_summary) -> LearningReport` | **GREEN** | Built T3 (`learning_observer.py`, B1–B6 fixes) with TEST-learning_observer.py 7/7 PASS; wired into factory T4; injection ungated on mtr_engine presence T8. Single post-answer learning socket. **Fail-loud emission (2026-07-12, SPEC_OBSERVER_FAIL_LOUD.md):** violation emission no longer `except: pass` — on exception or writer backpressure it records `report.violation_error` (and routes to the orchestrator DiagnosticFeed via `LEARNING_OBSERVER`), leaving `report.error` None and the pipeline (trace/feedback/phantom) running. Fact attribution is now deterministic (`returned_fact_id = min(fact_ids)`, not `next(iter(...))`). TEST-learning_observer.py 10/10 PASS (7 B-fixes + 3 fail-loud/determinism). |
| MTR engine | `KitbashMTREngine.forward(tokens, state, target_layer, kappa)` + `get_epistemic_snapshot` (instance method) | **GREEN** | `TEST-MTR_v6_1_contract.py` executed on real hardware (torch 2.13.0): 10/10 PASS (2026-07-10). Fixed error_signal shape bug: `MTREbbinghausLayer` emitted `(batch, seq_len, 1, 1)`; corrected to `(batch, seq_len, 1)` (matches docstring + test). v6 in place = RED; do not deploy v6. |
| Epistemic layer names | `MTR_v6_1.LAYER_NAMES` (single source of truth) | YELLOW | Consumers (`mtr_grain_bridge` weights dict) still on stale names until the one-line diff lands + contract test. |
| GrainRouter.search_grains | `(concepts, recent) -> [(grain_id, score)]` | **GREEN (2026-07-11)** | FIXED 2026-07-10: `search_grains` now scores keyword/token overlap (recall) between `query_concepts` and `grain["text"]` as the dominant signal (scoped to scoring loop; no grain storage change, no embeddings). Harness MRR 0.131 -> **0.4140** (dev) / **0.4164** (holdout) on 200x50. Regime: R@1=0.045 but R@3=0.815/R@5=0.935/meanRk=3.87 — query-correlated but NOT precisely ordered (lexical trap owns #1). This is the intended "noisy first-pass filter," NOT precise ranking (MTR's job). Does not warrant the precision flag. Stays YELLOW: should be re-confirmed once the grain registry format contract (adjacent cell) lands and real (non-synthetic) grains carry text. **GREEN (2026-07-11):** `TEST-grain_router.py` 4/4 PASS — query-conditioned ranking (matching-concept grain outranks non-matching), unrelated query re-ranks, no-overlap safe + score-descending, recent_grains graph boost = +0.20. Cell YELLOW→GREEN. |
| MTR↔Grain bridge | `MTRGrainUnifiedPipeline.process_mtr_query(...)` | RED | Soft-fail patterns (`dict.get` on layer names, bare `except: pass` in concept extraction); salience>0.3 gate calibrated to sigmoid regime. Fail-loud sweep target. |
| HatKappaMapper | `get_kappa(hat) -> float` | YELLOW | The L4→routing rigidity channel. Severed in v6, restored in v6.1; needs one contract assertion (kappa reaches MTR — in orchestrator contract suite Step 3). |
| Phantom tracker / crystallization | `advance_phantom_cycle`, crystallize-at-interval | **GREEN** | B1/B2 fixed inside LearningObserver (T3); TEST-learning_observer.py asserts single counter (B1) + single advance (B2), 7/7 PASS. T7 #6 asserts exactly one cycle-advance per query on the real path. 51-query cadence now the real cadence. |
| Resonance weights | `record_pattern / reinforce_pattern / advance_turn` | **GREEN** | Single-owner rule honored (orchestrator owns; observer does not touch). Exercised: T7 #4 asserts `record_pattern` once per answered query; real run in T8. |
| MTR state manager | `MTRStateCheckpoint.save/load/exists` | **GREEN** | F1 resolved: factory loads checkpoint (T5) AND seeds it into the observer (T8) so the counter resumes. TEST-factory_smoke.py 6/6 (T5) + T8 e2e asserts persist-on-close + resume-on-rebuild (time 19→27). |
| L2 Working Theory service | `L2WorkingTheoryService` (read-only audit) | **GREEN (2026-07-11)** | Canonical wiring landed via `query_orchestrator_factory.py` — replaced hardcoded `l2_service=None` with real `L2WorkingTheoryService(dream_bucket_dir=..., grain_orchestrator=...)` (ported from donor `attic/query_orchestrator.py:280-284`). `TEST-mutation2_l2_wiring.py` verifies: factory produces non-None instance, observer receives it, `get_working_theory_snapshot()` returns real audit data, real `process_query()` runs without crash. **INVOCATION GAP (flagged, out of scope):** `LearningObserver.observe()` stores `self.l2_service` but does NOT yet call it, so a query does not currently produce L2 audit output from within observe(). Wiring complete; invocation belongs to Mutation 3 / Phase 5B (L2 enrichment). |

## 3. Sleep Plane (the consolidating path)

| Socket | Interface | Status | Evidence / Notes |
|---|---|---|---|
| Dream Bucket write | `DreamBucketWriter` + `log_false_positive / log_consistency_violation / log_hypothesis / log_trace` | **GREEN** | Canonical path now writes: observer emits one trace/query (chain==fact_ids, chain_type truthful, context JSON-serializable) — T7 #8 asserts shape; T3 B3/B4 fixed AttributeError + per-query chain labeling. Append-only JSONL. **PERSISTENCE ROOT-CAUSE FIXED 2026-07-11 (commit 5092d86, post-map-mtime):** the live `trace_logged=False` break was Bug 1 — factory built `dream_bucket_writer` but OMITTED it from the `LearningObserver(...)` call, so `observer.dream_bucket_writer=None` → `_log_trace()` returned False and traces never queued. Fixed by passing `dream_bucket_writer=...` into the observer ctor. Bug 2 (async daemon-thread queue lost on CLI process exit) fixed via `DreamBucketWriter.close()` (sentinel+join), `orchestrator.close()` now drains writer, `kitbash_cli.py` calls `orchestrator.close()` in `finally`. Verified (`hermes-verify-trace-flush2.py`): `observer.writer present=True`, `trace_logged=True`, `traces.jsonl` 27→28 (+1 persisted through close()). The earlier EOD "process-exit loses queued traces" theory was only Bug 2; Bug 1 was the real `trace_logged=False` cause all along. |
| Dream Bucket read | `DreamBucketReader` | YELLOW | Consumed by sleep stages. |
| Sleep pipeline stages (1, 1.5, 2–6) | `SleepOrchestrator.run_stage_N() -> report dict` | YELLOW | Per-stage try/except with error recorded in report — acceptable soft-fail (errors ARE surfaced). No contract tests per stage; synthetic-bucket fixtures (`generate_synthetic_dream_bucket.py`) make these cheap to write. |
| Mutation 5 — Axiom-Aware Recalibration (`sleep_recalibration_service.py`) | Stage 5 `run_recalibration_cycle` | **YELLOW (was wrongly GREEN)** | **CORRECTION 2026-07-10 (SPEC_AXIOM_RECALIBRATION.md):** prior GREEN was docstring/uniform-treatment framing, NOT verified. Reality: (F1/F3/F4 grain-side) **NOW IMPLEMENTED — Step 2 done (commit e64a154 + step2 test)**: `_apply_feedback` resolves `returned_fact_id` via the 1:N `grain_by_fact` (Mutation 1) and applies axiom/observation asymmetry per grain — observations decrement on signal > 0.3, axioms flag via `log_hypothesis(subtype=contradiction)` only above `AXIOM_CONTRADICTION_THRESHOLD=0.5` (reuses MTR's high-error band), never decrement below it. `TEST-recalibration_grain_step2.py` 16/16 PASS. (F2 edge side) still BLOCKED: the "average penalty to every edge" logic is neutralized to a guarded no-op-and-report; real per-edge targeting needs the violation-schema gap (line 83) resolved. F2 was believed to cause live damage; verifiably was NOT (no edge graph on disk). Spec closed partial: grain-side Steps 2 done, F2 edge targeting (Step 1) + Step 3 reconciliation consumption still pending the violation-schema gap. |
| Procedural edge extractor | Stage 1.5, consumes trace chains | YELLOW | Contract must be reconciled with B4 fix: define what a chain IS (per-query) in one place both writer and extractor import. |
| Sleep-time training (gate/temp/prior) | triples: (trace, snapshot, outcome) | PLANNED | PROPOSAL_BOUNDARY_GATED_ROUTING §5. Prerequisite-gated. |
| Nightly auto-experiment stage | harness run appended to sleep cycle | PLANNED | "The system dreams experiments about itself." Cheap once harness adapters are wired. |

## 4. Bus Plane (RedisBlackboard — the extension fabric)

The bus is a **socket factory**: future components attach here rather than requiring orchestrator surgery. Current method families: query lifecycle (`create/get/update/delete/enqueue/dequeue`), grain store, diagnostic feed, worker health, metrics — all under `kitbash:` prefix.

| Socket | Status | Notes |
|---|---|---|
| RedisBlackboard core | **GREEN (2026-07-12)** | Clean API; envelope + TTLs wired (SPEC §1/§4: `_wrap`/`_unwrap`, loud-reject on bad version, query 24h / health 5m / metrics 7d) — verified ad-hoc against fakeredis 14/14 (2026-07-10). **Wired into canonical path 2026-07-12:** `query_orchestrator_factory` now builds `RedisDiagnosticFeed` (implements the DiagnosticFeed interface: log_query_created/started/completed, log_layer_hit/miss, log_error, log_metric) against the canonical `kitbash-redis` container (localhost:6379) and passes it as `diagnostic_feed`; the same live client is shared as `redis_client` so `CouplingValidator` auto-connects. Graceful no-op degrade: if Redis is unreachable, the feed logs a warning and becomes a no-op (orchestrator still starts + answers) — verified both paths. **Contract suite:** `TEST-redis_feed.py` PASSED (live kitbash-redis: factory builds feed alive + all 7 feed calls persist as well-formed `diagnostic_event@1` envelopes; dead-Redis path no-op, no raise). CAVEAT: only the diagnostic-feed surface is on the canonical path so far; worker-health / metrics / coupling validation via the bus are still partial (those cells YELLOW). |
| Coupling validator | YELLOW | `CouplingDelta` schema + Lua scripts; orchestrator already consumes deltas when present. |
| Intentional data stream format | **GREEN (RATIFIED 2026-07-10)** | SPEC `docs/SPEC_STREAM_FORMAT.md` (envelope §1, registry §2, schemas §3, TTL §4). Wired + contract-tested: blackboard envelope+TTLs (Part 1), coupling GAP-1 namespace (Part 2), epistemic_snapshot writer (§3.8), `TEST-bus_stream_format.py` 20/20 (fakeredis + live-Redis coupling Lua path). One open flag: epistemic TTL is a **provisional 24h** (§4 has no epistemic row) — confirm before final ratification; does not block GREEN. |
| Worker health registry | YELLOW | `set/get_worker_health`, `all_workers_healthy` — this is the attach-point every future worker registers with. |

**Bus attachment protocol (requirement for ALL future plug-ins):** any new component attaching to the bus must declare, in its own header and in a new row here: (1) its key namespace under `kitbash:<component>:`, (2) the schema version of every payload it reads/writes (versioned from day one — the stream-format spec defines the envelope), (3) TTL/archival policy for its keys, (4) worker-health registration, and (5) a contract test that runs against a local Redis (or fakeredis) proving read/write round-trip. A bus attachment without these five is a v6 waiting to happen. **Reserved rows:** SLM-v3 persistence layer (PLANNED — decision pending: sleep-consolidation vs query-time parallel), microspecialist NNs (PLANNED — anaphora/sense-disambiguation workers), BitNet synthesis worker (PLANNED), real Mamba context service (PLANNED), handshake LoRA manager (PLANNED — gated on stable sleep signal).

## 5. Storage Plane

| Socket | Interface | Status | Notes |
|---|---|---|---|
| Cartridge files (.kbc) | `kitbash_cartridge` / `kitbash_registry` load-store | YELLOW | Hot/cold loading planned alongside Redis wiring. Format is a contract: version field status unverified — check before Phase 5 wiring. |
| Grain registry persistence | `grain_registry` disk format | **GREEN (Mutation 1 DONE 2026-07-10)** | Format contract test written BEFORE the mutation (per discipline): `TEST-grain_format_contract.py` **14/14 PASS** (commit `e64a154`). `GrainMetadata` gained `fact_ids` field + `from_dict`; `to_dict` persists `fact_ids`/`grain_type`/`confidence`/`epistemic_level`; `GrainRouter.grain_by_fact` flipped to `Dict[int, List[str]]` (1:N accumulate). Legacy grains load. No MTR regression (10/10). |
| MTR checkpoints | state dict on disk | YELLOW | Layer-name-free (verified in v6.1 audit) — version-portable by construction. Keep it that way; add assertion to MTR contract suite. |
| SQLite stores | various | YELLOW | Not audited this pass. |
| **GAP — Grain schema lacked `fact_id` AND `grain_type`** | on-disk grain files (`grains/*.json`) | **RESOLVED via Mutation 1 (2026-07-10)** | Was: grains carried keys `[axiom_ids, cartridge_id, epistemic_level, grain_id, num_bits, quality_metrics, size_bytes, source_phantom_id, state, weight_distribution]` — no `fact_id`, no `grain_type`. **Resolved:** `GrainMetadata` gained `fact_ids: List[int]` field + `from_dict` (re-triggers `__post_init__` invariants on reload); `to_dict` persists `fact_ids`/`grain_type`/`confidence`/`epistemic_level`; `GrainRouter.grain_by_fact` flipped `Dict[int, str]` → `Dict[int, List[str]]` (1:N accumulate, built from `fact_ids` list — was 1:1 last-wins). Contract `TEST-grain_format_contract.py` 14/14 PASS (commit `e64a154`). `grain_type` split already in dataclass (Phase 5A). This UNBLOCKS F3 fact→grain write-back + §2.2 axiom/observation asymmetry in SPEC_AXIOM_RECALIBRATION. **Legacy-load note:** `__post_init__` observation band relaxed to allow `confidence==0.0` as UNSET/legacy so pre-Phase-5A grains load (axioms still strictly ≥0.95). Violation-schema gap (line 83) remains the only open blocker for F2 edge targeting. **TICKET CLOSED: opened from SPEC_AXIOM_RECALIBRATION.md Step 0; resolved by Mutation 1, commit e64a154.** |
| **GAP — Violation records carry no edge/fact-chain reference** | `data/subconscious/dream_bucket/live/violations.jsonl` | **RESOLVED → YELLOW (2026-07-12)** | Was RED BLOCKER: violation keys carried no edge/fact-chain ref, blocking F2 edge-targeting. **(b) MAPPING CLOSED (2026-07-12, commit de07f16→impl):** `LearningObserver.observe()` emits `context={"recent_fact_ids": [...]}` (gate `mtr_error > 0.5`; orphaned `MTR_v6_1.log_consistency_violation` deleted). `RecalibrationService._update_edge_weights` now implements the field→edge-key mapping per SPEC_F2_EDGE_TARGETING.md: targetable = non-empty `context.recent_fact_ids`; implicated edge iff `source_fact_id`/`target_fact_id` ∈ recent_fact_ids; penalty `min(mtr_error_signal*0.15, 0.1)`, floor 0.0, non-destructive; persists via `_save_edge_graph`. `TEST-recalibration_f2_targeted.py` GREEN (exit 0, count=2, unrelated edge unchanged). **(a) STILL OPEN — cell stays YELLOW:** `procedural_edge_graph.json` does NOT exist on disk; the sleep extractor (Stage 1.5/2.5) must RUN over the now-persisted `traces.jsonl` (trace-logging fixed `5092d86`) to BUILD it. Until the live graph exists, `_update_edge_weights` no-ops with `reason='no_edge_graph_on_disk'`. Also gated on the mtr_error signal-validation ticket (DIAGNOSTIC-trace_signal_check Q1) per spec non-goal #4. MTR↔Grain bridge (separate RED) unaffected. **MEASURED 2026-07-13 (TASKS_POST_HARDENING_CLOSEOUT Task B, read-only):** live `violations.jsonl` = 67 records; split by `context.recent_fact_ids`: 3 POST-`0978672` (new schema, targetable) / 64 PRE (old deleted emitter, no recent_fact_ids — F2-untargetable). Live signal ranges (DIAGNOSTIC-trace_signal_check.py): violation `mtr_error_signal` n=67 min=0.45 max=**8.77** mean=0.93 (values in the ~8 range are REAL, not artifact — confirms the unvalidated-signal problem); `returned_confidence` 3 negatives (min=-7.77); trace `confidence` many negatives (~-8.6). grain_ids 0% populated on 566 live records. Corpus is synthetic (5 synthetic_session_*, 32 distinct query_ids, 3 fact_ids). Calibration decision is Isaac's — no clamp/normalize applied this turn. **Task C (2026-07-13) STEP-0 GATE STOP:** `sleep_procedural_edge_extractor.py` expects `trace['chain']` = a **list of step dicts** (`{fact_id, cartridge}`), iterating `chain[i].get('fact_id')` (lines 112-127). Real trace `chain` (B4 shape) = a **per-query dict** `{"query_id","chain_type","fact_ids":[int],"grain_ids":[],"confidence"}` (verified on one live record: `type(chain)=dict`, no `fact_id` key at top, `fact_ids=[1]`). Shapes do NOT match — forcing the run would crash (`.get` on str) or yield 0/garbage edges. Per the gate, halted; no graph built; no extractor/record adapted. The real fix is the SOCKET_MAP-pending "define what a chain IS in one place both writer and extractor import" — out of Task C scope. `procedural_edge_graph.json` still ABSENT; follow-up (a) stays OPEN (gated on Isaac's calibration decision before any live Stage 5). **RESOLUTION IN FLIGHT (2026-07-13):** `SPEC_TRACE_CHAIN_CONTRACT.md` defines the canonical chain (`interfaces/trace_chain.py`: `TraceChain` dataclass + `iter_cooccurrence_edges` — pairwise co-occurrence over `set(fact_ids)`, canonical `a<b`, order-independent; consecutive-pair semantics forbidden since `fact_ids` is set-sourced). Stage 2.5 cross-query edges removed from scope (deferred ticket). Phase 1 landed (module + `TEST-trace_chain_contract.py` 7/7 PASS); Phase 2 (wire writer+extractor) pending HARD STOP. |

## 6. Evaluation Plane

| Socket | Interface | Status | Notes |
|---|---|---|---|
| Ranking harness | `TEST-ranking_harness.py` Ranker protocol: `rank(query, candidates) -> permutation` | **GREEN** | Self-check executed passing 2026-07-07 (9/9); sealed boundary, anti-cheat, provenance log. The only green cell — that is the honest baseline. |
| grain_router harness adapter | wired | GREEN | Executed against live read-only code; produced the RED measurement above. |
| mtr_pipeline harness adapter | skeleton + wiring guide | PLANNED | Needs: ranked-fact call chain + ingestion route. **The MTR-vs-ablated pair is the money experiment.** |
| MTR contract suite | `TEST-MTR_v6_1_contract.py` | **GREEN** | Executed on real hardware (torch 2.13.0): 10/10 PASS (2026-07-10). Was YELLOW awaiting first hardware run; now run. |
| Orchestrator contract suite | spec §5 | **GREEN (2026-07-12)** | SPEC_CONTRACT_SUITE_MAMBA_REPAIR.md Phase 1+2 complete. Suite now **23/23 PASS** on real hardware: #1–#6, #7 (factory coherence identity `a is b`, classified sandbox artifact), and #8 (trace context JSON-serializable — fixed by orchestrator-only `_serializable_mamba_context` at line 198, which excludes `hidden_state` bytes and ISO-8601-stringifies `conversation_history` timestamps). No answer/routing text changed (Pattern A untouched; `context["mamba_context"]` has zero readers — grep-verified). **Answer-path proof now ALSO complete:** `TEST-orchestrator_e2e.py` 10/10 PASS on real torch (TASKS_POST_HARDENING_CLOSEOUT Task A — the pre-existing missing `from pathlib import Path` import is fixed; ticket CLOSED). |

---

## Phase 5 Critical Path (the cells that define "yak shaved")

In dependency order: **Query entry (GREEN) → LearningObserver (GREEN) → MTR v6.1 (GREEN: TEST-MTR_v6_1_contract.py 10/10 on hardware) → stream format spec (GREEN, RATIFIED 2026-07-10: docs/SPEC_STREAM_FORMAT.md + TEST-bus_stream_format.py 20/20) → RedisBlackboard wiring (GREEN 2026-07-12: RedisDiagnosticFeed on canonical path + TEST-redis_feed.py PASSED; worker-health/metrics/coupling still partial YELLOW) → grain registry format contract (GREEN, Mutation 1) → L2 service (Mutation 2 — GREEN 2026-07-11).** When every cell on this line is GREEN, refactoring is over and building resumes. Everything else on this map is backlog by definition — including things that are genuinely broken. That is the license to stop looking.

## Maintenance Rules

1. This document changes in the same commit/session as any socket it describes. A socket change without a map change is a spec violation.
2. Initial statuses above are from a single Fable audit pass of read-only copies; anything marked *unverified* gets verified the first time a task touches it, not preemptively.
3. Re-audit cadence: after each Phase-blocker completes, one pass to catch drift. Otherwise the map is event-driven, not scheduled — a scheduled full re-audit is the yak-shave spiral wearing a badge.
